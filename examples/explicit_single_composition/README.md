# Explicit Example --- Single Composition

This example runs the full workflow (00--07) for one explicit co-doped
composition.

## Run

```bash
dopingflow run-all -c input.toml
```

Results will be written to: 
- random_structures/ 
- results_database.csv